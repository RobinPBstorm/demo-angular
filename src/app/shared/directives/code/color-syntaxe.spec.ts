import { ColorSyntaxe } from './color-syntaxe';

describe('ColorSyntaxe', () => {
  it('should create an instance', () => {
    const directive = new ColorSyntaxe();
    expect(directive).toBeTruthy();
  });
});
